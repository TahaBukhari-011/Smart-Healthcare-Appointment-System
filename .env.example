# ===========================================
# ENVIRONMENT VARIABLES CONFIGURATION
# ===========================================
# 
# INSTRUCTIONS:
# 1. Copy this file and rename it to `.env.local`
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit `.env.local` to version control
#
# ===========================================

# ---------------------------------------------
# MongoDB Connection
# ---------------------------------------------
# For local development: mongodb://localhost:27017/healthcare
# For MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/healthcare?retryWrites=true&w=majority
#
# Get free MongoDB Atlas cluster at: https://www.mongodb.com/atlas
#
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/healthcare?retryWrites=true&w=majority

# ---------------------------------------------
# JWT Secret
# ---------------------------------------------
# Used for signing authentication tokens
# IMPORTANT: Use a strong, random string (minimum 32 characters)
#
# Generate one using:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-change-this

# ---------------------------------------------
# App URL (Optional)
# ---------------------------------------------
# Your application's public URL
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
#
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ---------------------------------------------
# Environment
# ---------------------------------------------
NODE_ENV=development
